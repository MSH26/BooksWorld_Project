@model BooksWorld.Models.LoginModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Html.Raw(@TempData["msg"])

<fieldset>
    <legend><b>LOGIN</b></legend>
    <form method="post">
        <br/>
        <table>
            <tr>
                <td>User Name</td>
                <td>:</td>
                <td><input type="text" name="userName" value="MSH"></td>
            </tr>
            <tr>
                <td>Password</td>
                <td>:</td>
                <td><input type="password" name="password" value="555"></td>
            </tr>
        </table>
        <hr />
        <input name="remember" type="checkbox">Remember Me
        <br/><br/>
        <input type="submit" value="Submit">
		@Html.ActionLink("Forgot Password", "ForgotPassword", "Home")
    </form>
</fieldset>